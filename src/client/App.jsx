import React from 'react';

const App = () => (
  <div>
    <h1>I am a hot reloadable React component!</h1>
    <p>Change me and watch the page update without a refresh needed.</p>
    <a href="/api">The backend is hot reloadable too</a>
  </div>
);

export default App;
